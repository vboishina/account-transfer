spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  liquibase:
    enabled: true

fbi:
  auth:
     api-key: ${FIB_API_KEY:test-api-key}